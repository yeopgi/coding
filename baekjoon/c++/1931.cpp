#include <iostream>
#include <vector>
using namespace std;

vector<vector<int>> Information;

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int meeting;
    cin >> meeting;
    while (meeting--) {

    }

    return 0;
}