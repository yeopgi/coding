#include <iostream>
using namespace std;

int list[21];

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int N, S;
    cin >> N >> S;
    for (int i = 1; i <= N; i++) {
        cin >> list[i];
    }

    

        return 0;
}