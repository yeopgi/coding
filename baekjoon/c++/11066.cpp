#include <iostream>
using namespace std;

int T;
int input[501];

int main(void)
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    cin >> T;
    while (T--) {
        int cnt;
        cin >> cnt;
        for (int i = 1; i <= cnt; i++) {
            cin >> input[i];
        }

        
    }

    return 0;
}